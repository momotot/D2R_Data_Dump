#pragma once

#include <iostream>
#include <string>
#include <Windows.h>
#include <fstream>
#include <chrono>
#include <algorithm>

#pragma comment(lib, "Version.lib")

std::wstring s2ws(const std::string& s);